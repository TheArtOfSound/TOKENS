node_modules/
dist/
.env
.env.*
!.env.example
.DS_Store
*.log
collector/local-logs/
collector/local-qira-projects.json
public/data/raw/
tsconfig.tsbuildinfo
.tmp-bin/
.tokens-cache/

# From the origin lineage -- kept: these also protect the Ed25519 device key.
*.env
credentials.json
*.pem
*.key
.credentials

# Personal profile config — each member's own identity, never shipped in a clone.
# A fresh clone would otherwise publish the previous owner's name and rates.
profile/*.json
!profile/*.example.json
